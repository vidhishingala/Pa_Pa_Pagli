<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pa_pa_pagli">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pa_Pa_Pagli">
        <activity android:name=".FruitsDetailActivity"></activity>
        <activity android:name=".FruitsActivity">

        </activity>
        <activity android:name=".FamilyDetailActivity" />
        <activity android:name=".FamilyActivity"></activity>
        <activity android:name=".BirdsDetailActivity" />
        <activity android:name=".BirdsActivity" />
        <activity android:name=".AnimalDetailActivity" />
        <activity android:name=".AnimalActivity" />
        <activity android:name=".FlowerActivity"></activity>
        <activity android:name=".FlowerDetailActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>